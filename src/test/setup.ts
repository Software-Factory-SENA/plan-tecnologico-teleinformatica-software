// ── Configuración global de tests ────────────────────────────
// Extiende los matchers de Vitest con los de @testing-library/jest-dom
// para poder usar: toBeInTheDocument, toHaveAttribute, toBeVisible, etc.

import "@testing-library/jest-dom";
